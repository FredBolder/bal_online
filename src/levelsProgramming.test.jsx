import { describe, it, expect } from "vitest";

describe("Ideas for levels about programming", () => {
    it("For loop A", () => {
        let n = 0;
        for (let i = 0; i <= 3; i++) n++;
        expect(n).toEqual(4);
    });

    // it("Comparison A", () => {
    //     expect({ x: 4, y: 3 } === { x: 4, y: 3 }).toEqual(false);
    // });

    // it("Comparison B", () => {
    //     expect([1, 2] !== [1, 2]).toEqual(true);
    // });

    // Insert new tests here
});